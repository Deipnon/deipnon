// @flow
import * as React from 'react'
import { Typography } from 'antd'

const { Title } = Typography

const Home = () => {
	return (<React.Fragment>
		<Title className="page-title"level={3}>Dashboard</Title>
	</React.Fragment>)
}

export default Home
